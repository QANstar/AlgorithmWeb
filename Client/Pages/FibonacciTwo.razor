@page "/fibonacci/two"
@using AlgorithmWeb.Shared
@inject HttpClient Http;
@if (isLoading)
{
    <div class="loadingBox">
        <img class="imgAnim" src="/paiMeng.webp" />
        <div id="loading">
            <div class="l l1"></div>
            <div class="l l2"></div>
            <div class="l l3"></div>
		</div>
    </div>
}
else
{
    <div  class="showAnim">
        <div style="margin-bottom:3vh">
            <a href="fibonacci"><Button Color="Color.Info">返回</Button></a>
        </div>
        <div style="margin-top:10vh;justify-content: center;" class="row">
            <div class="col-md-4">
                <Card Color="Color.Primary" IsCenter="true">
                <CardHeader>
                    迭代计算
                    <a @onclick="iterationMaxIntClick" class="abRight">计算</a>
                </CardHeader>
                <CardBody>
                    @if (isLoading1)
                    {
                        <div class="loadingBox" style="height:100%">
                            <img class="imgAnim" src="/paiMeng.webp" />
                            <div id="loading">
                                <div class="l l1"></div>
                                <div class="l l2"></div>
                                <div class="l l3"></div>
	                        </div>
                        </div>
                    }
                    else
                    {
                        @if (isSum1)
                        {
                            <ul class="textLeft" > 
                                <li>结果：@iteraFibMode.resultNum</li>
                                <li>第几个斐波那契数：@iteraFibMode.num</li>
                                <li>执行时间：@iteraFibMode.time</li>
                            </ul>
                    }
                        else
                        {
                            <ul class="textLeft" > 
                                <li>结果：未计算</li>
                                <li>第几个斐波那契数：未计算</li>
                                <li>执行时间：未计算</li>
                            </ul>
                        }

                    }
                       
                </CardBody>
            </Card>
            </div>
            <div class="col-md-4">
                <Card Color="Color.Warning" IsCenter="true">
                <CardHeader>
                    递归计算
                    <a @onclick="recursionMaxIntClick" class="abRight">计算</a>
                </CardHeader>
                <CardBody>
                    @if (isLoading2)
                    {
                        <div class="loadingBox" style="height:100%">
                            <img class="imgAnim" src="/paiMeng.webp" />
                            <div id="loading">
                                <div class="l l1"></div>
                                <div class="l l2"></div>
                                <div class="l l3"></div>
	                        </div>
                        </div>
                    }
                    else
                    {
                        @if (isSum2)
                        {
                            <ul class="textLeft" > 
                                <li>结果：@recursionFibMode.resultNum</li>
                                <li>第几个斐波那契数：@recursionFibMode.num</li>
                                <li>执行时间：@recursionFibMode.time</li>
                            </ul>
                    }
                        else
                        {
                            <ul class="textLeft" > 
                                <li>结果：未计算</li>
                                <li>第几个斐波那契数：未计算</li>
                                <li>执行时间：未计算</li>
                            </ul>
                        }

                    }
                </CardBody>
            </Card>
            </div>
        </div>
        <div style="margin-top:5vh;justify-content: center;" class="row">
            <div class="col-md-4">
                <Card Color="Color.Success" IsCenter="true">
                <CardHeader>
                    利用迭代的结果递归计算时间
                    <a @onclick="recursionClick" class="abRight">计算</a>
                </CardHeader>
                <CardBody>
                    @if (isLoading3)
                    {
                        <div class="loadingBox" style="height:100%">
                            <img class="imgAnim" src="/paiMeng.webp" />
                            <div id="loading">
                                <div class="l l1"></div>
                                <div class="l l2"></div>
                                <div class="l l3"></div>
	                        </div>
                        </div>
                    }
                    else
                    {
                        @if (isSum3)
                        {
                            <ul class="textLeft">
                              <li>结果：@recursionFibMode2.resultNum</li>
                              <li>执行时间：@recursionFibMode2.time</li>
                              <li>能否在1分钟内完成：@inOneMin</li>
                            </ul>
                    }
                        else
                        {
                            <ul class="textLeft" > 
                                <li>结果：未计算</li>
                                <li>执行时间：未计算</li>
                                <li>能否在1分钟内完成：：未计算</li>
                            </ul>
                        }

                    }
                    
                   
                </CardBody>
            </Card>
            </div>
        </div>
    </div>
}